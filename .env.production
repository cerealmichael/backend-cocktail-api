# Production Environment Variables Template for Railway
# Railway will inject these automatically from the service variables

TZ=UTC
PORT=$PORT
HOST=0.0.0.0
LOG_LEVEL=info
NODE_ENV=production

# Generate a secure APP_KEY using: node ace generate:key
APP_KEY=$APP_KEY

# Database - Railway PostgreSQL (these will be auto-injected)
DB_HOST=$PGHOST
DB_PORT=$PGPORT
DB_USER=$PGUSER
DB_PASSWORD=$PGPASSWORD
DB_DATABASE=$PGDATABASE

# Drive configuration
DRIVE_DISK=fs

# App domain (Railway will provide this)
APP_DOMAIN=$RAILWAY_PUBLIC_DOMAIN
